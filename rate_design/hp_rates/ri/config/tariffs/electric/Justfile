# Standalone Justfile for RI electric Genability tariff fetch. Run from repo root:
#   just -f rate_design/hp_rates/ri/config/tariffs/electric/Justfile fetch-genability
# Prefer the shared recipe instead: UTILITY=rie just s ri fetch-genability-tariffs
path_project_root := `git rev-parse --show-toplevel`
path_output_dir := path_project_root + "/rate_design/hp_rates/ri/config/rev_requirement/top-ups"
path_genability_yaml := path_output_dir + "/tariffs_by_utility.yaml"

# Fetch all RI electric default tariffs from Genability/Arcadia; writes <tariff_key>.json to top-ups/default_tariffs/.
fetch-genability:
	uv run --directory "{{path_project_root}}" python {{path_project_root}}/utils/pre/fetch_electric_tariffs_genability.py "{{path_genability_yaml}}" "{{path_output_dir}}/default_tariffs" --state RI
